name: Workflow
on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu
    steps:
      - name: echo a string
        run: echo "hello world"
      - name: Multiline command
        run: |
          node -v
          npm -v
          

